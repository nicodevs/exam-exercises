By making use of `defineProps` inside a `setup` function:

```
<template>
  <h1>{{ msg }}</h1>
</template>

<script>
import { defineProps } from 'vue';

export default {
  setup(props) {
    defineProps({
      msg: String,
    });

    return {
      msg: props.msg
    };
  }
};
</script>
```
